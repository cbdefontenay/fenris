name: structurizer
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  rust_lib_structurizer:
    path: rust_builder
  flutter_rust_bridge: 2.11.1
  shared_preferences: ^2.5.3
  flutter_markdown: ^0.7.7+1
  flutter_html: ^3.0.0
  flutter_highlight: ^0.7.0
  highlight: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true